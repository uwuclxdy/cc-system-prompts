name: ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # exact, not a floating major: astral-sh stopped publishing one past v7
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true

      - run: uv sync --frozen
      - run: uv run ruff check
      - run: uv run ruff format --check
      - run: uv run pytest
      # pinned through .shellcheck-version, which the local commit gate reads
      # too, so the two verdicts cannot drift: ubuntu-24.04 ships 0.9.0, which
      # flags SC3013 and SC2015 that 0.11.0 does not, and an unpinned gate would
      # red on a runner image bump alone
      - name: shellcheck
        run: |
          ver=$(cat .shellcheck-version)
          curl -fsSL -o "$RUNNER_TEMP/shellcheck.tar.xz" \
            "https://github.com/koalaman/shellcheck/releases/download/${ver}/shellcheck-${ver}.linux.x86_64.tar.xz"
          tar -xJf "$RUNNER_TEMP/shellcheck.tar.xz" -C "$RUNNER_TEMP" "shellcheck-${ver}/shellcheck"
          "$RUNNER_TEMP/shellcheck-${ver}/shellcheck" --version
          "$RUNNER_TEMP/shellcheck-${ver}/shellcheck" -- scripts/*.sh shim/claude
